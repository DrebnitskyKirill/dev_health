# Dev Health - Приложение для здоровья разработчиков

Приложение для мониторинга здоровья разработчиков с геймификацией и соревнованиями между коллегами.

## 🚀 Возможности

### Основные функции
- **Мониторинг осанки** - Отслеживание правильной осанки во время работы
- **Напоминания о моргании** - Регулярные напоминания о 20-20-20 правиле
- **Помодоро таймер** - Таймер для продуктивной работы
- **Режим работы** - Специальный режим для фокусированной работы

### Геймификация
- **Система достижений** - Бейджи за здоровые привычки
- **Уровни и опыт** - Прогресс пользователя
- **Лидерборд** - Соревнования между коллегами
- **Статистика** - Детальная аналитика активности

### Достижения
- 🍅 "Первые шаги" - Заверши свой первый помодоро-сеанс
- 🧍 "Стой прямо!" - Проведи 1 час с хорошей осанкой
- 👁️ "Не забывай моргать" - Получи 10 напоминаний о моргании
- 📅 "Неделя здоровья" - 7 дней подряд с хорошей осанкой
- ⏰ "Мастер времени" - Проведи 8 часов в рабочем режиме
- 🦾 "Железная спина" - 100 часов с хорошей осанкой
- 👑 "Легенда продуктивности" - 1000 помодоро-сеансов

## 🛠 Технологии

### Backend
- **Node.js** - Серверная платформа
- **Express** - Веб-фреймворк
- **TypeScript** - Типизированный JavaScript
- **PostgreSQL** - Реляционная база данных
- **Sequelize** - ORM для работы с базой данных
- **JWT** - Аутентификация
- **bcrypt** - Хеширование паролей

### Frontend
- **React** - UI библиотека
- **TypeScript** - Типизированный JavaScript
- **Tailwind CSS** - CSS фреймворк
- **React Router** - Маршрутизация
- **Context API** - Управление состоянием

## 📦 Установка

### Предварительные требования
- Node.js (версия 16 или выше)
- PostgreSQL (версия 12 или выше)

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd dev_health
```

### 2. Установка зависимостей
```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

### 3. Настройка базы данных
Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE dev_health;
```

### 4. Настройка переменных окружения
Создайте файл `.env` в папке `backend`:
```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dev_health
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your-secret-key

# Сервер
PORT=3001
NODE_ENV=development
```

### 5. Инициализация базы данных
```bash
cd backend
npm run init-db
```

### 6. Запуск приложения
```bash
# Backend (в папке backend)
npm run dev

# Frontend (в папке frontend)
npm start
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/profile` - Получение профиля
- `PUT /api/auth/profile` - Обновление профиля

### Геймификация
- `GET /api/gamification/achievements` - Все достижения
- `GET /api/gamification/user-achievements` - Достижения пользователя
- `GET /api/gamification/leaderboard` - Лидерборд
- `GET /api/gamification/statistics` - Статистика пользователя
- `POST /api/gamification/update-statistics` - Обновление статистики

### Мониторинг
- `POST /api/posture` - События осанки
- `POST /api/blink` - События моргания

## 🎮 Как работает геймификация

### Система очков
- **Рабочее время**: 0.1 очка за минуту работы
- **Хорошая осанка**: 0.2 очка за минуту правильной осанки
- **Напоминания о моргании**: 1 очко за каждое напоминание
- **Помодоро сеансы**: 5 очков за каждый завершенный сеанс
- **Дни подряд**: 10 очков за каждый день подряд с хорошей осанкой

### Уровни
- Каждые 100 очков = новый уровень
- Уровень влияет на рейтинг в лидерборде

### Достижения
- Автоматическая проверка при обновлении статистики
- Разные типы: posture, blink, pomodoro, streak, time
- Редкость: common, rare, epic, legendary

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- Валидация входных данных
- Защита от SQL инъекций через Sequelize

## 📱 Использование

1. **Регистрация/Вход** - Создайте аккаунт или войдите в систему
2. **Мониторинг** - Включите отслеживание осанки и моргания
3. **Помодоро** - Используйте таймер для продуктивной работы
4. **Достижения** - Следите за прогрессом и получайте бейджи
5. **Соревнования** - Сравнивайте результаты с коллегами

## 🏗 Архитектура

### Backend структура
```
src/
├── config/
│   └── database.ts          # Конфигурация базы данных
├── models/
│   ├── User.ts              # Модель пользователя
│   ├── Achievement.ts       # Модель достижений
│   └── index.ts             # Экспорт моделей и связей
├── routes/
│   ├── auth.ts              # Роуты аутентификации
│   └── gamification.ts      # Роуты геймификации
├── services/
│   └── gamification.ts      # Бизнес-логика геймификации
├── scripts/
│   └── initDb.ts            # Инициализация базы данных
└── index.ts                 # Основной файл сервера
```

### Frontend структура
```
src/
├── app/
│   ├── App.tsx              # Главный компонент
│   └── routes/
├── features/
│   ├── achievements/        # Виджет достижений
│   ├── leaderboard/         # Виджет лидерборда
│   ├── pomodoro/            # Помодоро таймер
│   └── visionReminder/      # Напоминания о моргании
├── pages/
│   ├── auth/                # Страницы аутентификации
│   ├── dashboard/           # Главная страница
│   └── settings/            # Настройки
└── shared/
    ├── context/             # React Context
    └── ui/                  # UI компоненты
```

## 🚀 Развертывание

### Production
1. Настройте переменные окружения для production
2. Соберите frontend: `npm run build`
3. Соберите backend: `npm run build`
4. Запустите backend: `npm start`

### Docker (опционально)
```bash
# Создание образа
docker build -t dev-health .

# Запуск контейнера
docker run -p 3001:3001 dev-health
```
